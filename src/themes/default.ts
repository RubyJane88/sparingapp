import { createTheme } from "@mui/material";

export const defaultTheme = createTheme({
  palette: {
    primary: {
      main: "#872b00",
    },
    secondary: {
      main: "#1b1563",
    },
    background: {
      default: "#f5f5f5",
    },
  },
});
